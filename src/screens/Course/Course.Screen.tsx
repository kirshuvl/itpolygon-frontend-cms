import clsx from 'clsx'
import type { Component } from 'solid-js'
import styles from './Course.Screen.module.scss'
export const CourseScreen: Component = () => {
    return <div class={clsx(styles.header)}>CourseScreen</div>
}
